# RecruitBot Night Status Snapshot — 2026-04-22

## Green now
- `runtime_check.py` = healthy
- `smoke_web.py` = PASS
- `fb_safe_workflow_ui_smoke.py` = PASS
- Facebook safe workflow now sends `CSRF` correctly and runs through the protected runtime
- `TopStaff Israel` is active again
- live worker vacancy has been copied into `TopStaff Israel` as vacancy `#8`
- Telegram worker destination `@haifa_rabota` has been copied into `TopStaff Israel` as source `#20`
- Facebook manual-assisted destination has been copied into `TopStaff Israel` as source `#21`
- morning pilot campaign is now `campaign #5`:
  - name: `TopStaff Worker Pilot — Concrete Floors`
  - running: `true`
  - vacancy: `#8`
  - source: `#20`

## Intentionally stopped
- old auto campaign `#3` under `FloorDSGN` was paused
- `HB Concrete Pilot Flow` under `FloorDSGN` remains paused

## Morning truth
- the worker-acquisition pilot should now be treated as a `TopStaff Israel` path, not a `FloorDSGN` path
- Telegram is the real automatic morning path
- Facebook remains legal-only:
  - `Pages` sync
  - `Groups` manual-assisted
  - `Marketplace` manual-assisted

## One honest remaining risk
- the real Telegram posting outcome for the new `TopStaff` campaign still needs the active morning window to confirm the final send path under live conditions
