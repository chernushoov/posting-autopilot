# Handoff — Posting Autopilot (2026-06-15)

> For the MacBook reviewer agent + owner. State of prod the night before launch.
> Read `DO_NOT.md` first, then this. Single source of truth for what's live & what's left.

## TL;DR (RU)
Прод жив и проверен: **https://app.posting-autopilot.com** (7 контейнеров healthy).
Сегодня: проверил личный кабинет на иврите (скриншот каждого экрана, реальный аккаунт),
нашёл и починил утечки русского текста в HE/EN (биллинг-тарифы, дни недели в аналитике,
роль в команде). Плюс поймал и устранил **критический landmine с env-файлами** (см. ниже) —
из-за него web на пару минут падал в 502, а триал молча скакнул с 14 на 3 дня. Всё восстановлено.
Дальше по списку: RAM-лимиты, письмо об окончании триала, авто-папки групп, Alembic.

---

## MacBook — server access GRANTED (2026-06-15)
You (MacBookAir.lan) now have **root SSH to prod with your OWN key** — verified today:
`ssh -i ~/.ssh/posting_autopilot_hetzner root@167.233.98.210` → `/opt/posting-autopilot`.
Your key is independently revocable (separate from the iMac's; authorized_keys comment
`macbookair-deploy-2026-06-15`).

**BEFORE you deploy anything, read `DO_NOT.md` → "Deploy / infra". The #1 rule:
always export `APP_ENV_FILE=.env.prod` on every compose recreate** — a bare
`docker compose up -d` loads the stale `.env` → web 502 + trial 14→3 + missing
`DATA_ENCRYPTION_KEY`. This cost a prod 502 today; don't repeat it.

- Secrets live ONLY in `.env.prod` ON THE SERVER — read them there; never copy off, never commit.
- Your footer/social commit `7f3cab8` is on `main` but NOT yet deployed (template change → `build web`).
  Deploy it when ready. The iMac has deployed everything through `46530fe` (cabinet i18n, env fix,
  swap+mem_limits, trial reminders, theme groups).
- We share `main` — pull before you push (you already did this correctly today).

---

## What is LIVE & verified (prod)
- App: `https://app.posting-autopilot.com` — gunicorn + Caddy (HTTPS), 7 containers healthy
  (web, worker, scheduler, bot, postgres, redis, caddy).
- `main` HEAD baked into the web image; deploy = `git archive HEAD app | ssh tar -x` then rebuild
  (see DO_NOT.md for the exact, now-corrected command).
- Trial = **14 days** (`Config.trial_days()` verified in-container), `BILLING_ENABLED=False`
  (paywall dormant pre-launch — flip when Stripe keys land).
- PII-at-rest encryption ACTIVE and **key-consistent across web/worker/bot** (`DATA_ENCRYPTION_KEY`
  md5 `11c0cd8f…` identical on all three — cross-service decrypt works).

### Hebrew cabinet QA (done today)
- Tool: `scripts/qa_cabinet_he.cjs` — registers a throwaway account on prod, forces HE/RTL,
  screenshots all 11 SPA screens + wizard, captures JS console errors, flags Cyrillic leakage,
  then hard-deletes the account. Re-run anytime: `QA_TS=$(date +%s) node scripts/qa_cabinet_he.cjs`.
- Result: `lang=he dir=rtl`, shell renders, registration→cabinet→delete all work, **0 JS errors**,
  **10/11 screens clean**. The 1 remaining Cyrillic hit is `bot → Русский`, which is the bot
  reply-language *picker* (Auto/Русский/עברית/English) — an endonym, correct UX, NOT a bug.
- Fixed today (commit `f3e0385`): billing plan taglines+features, analytics weekday labels,
  team role, and the `Pro · триал` subtitle were hardcoded RU literals. Now `cabinet_data.py`
  emits i18n keys (`tk`/`fk`/`dk`/`rk`) beside RU fallbacks, `applyLang()` resolves them, and
  translations live in `cabinet-i18n.js` (RU/EN/HE) — so they re-translate on the live language switch.

## CRITICAL landmine fixed today — env files
`.env` (what containers actually load via `env_file: ${APP_ENV_FILE:-.env}`) had drifted STALE vs
`.env.prod` (the real config). A manual `docker compose up -d web` WITHOUT exporting
`APP_ENV_FILE=.env.prod` loaded the stale `.env` → web 502 (env_guard wants `PUBLIC_*_URL`),
trial 14→3, missing `DATA_ENCRYPTION_KEY`, wrong tokens. **Recovered**: web recreated with
`APP_ENV_FILE=.env.prod`; then hardened by making `.env` a byte-copy of `.env.prod` on the box
(backup `.env.bak.20260615-131040`). Always use the export anyway. Full detail in DO_NOT.md.

## This session's commits (newest first)
- `f3e0385` fix(cabinet): translate billing plans, analytics weekdays & team role in HE/EN
- `eba0c09` feat(auth): welcome email on signup + register rate-limit
- `6184032` feat(privacy): encrypt Candidate PII at rest (phone + chat_log_json)
- `7394389` feat(billing): worker paywall — stop auto-posting after trial expiry
- `5a05c70` feat(gdpr): data export + account deletion
- `a886ae4` / `20b1aec` feat(observability): env-driven Sentry (web + worker) + dep
- `217594f` feat(auth): password reset + provider-agnostic email
- `4c5bd0d` docs(launch): BATTLE_LAUNCH master + DO_NOT anti-patterns

## Owner-gated (I CANNOT do these — need scoped secrets, set ONLY in `.env.prod` on the box)
- **Stripe** keys (then flip `BILLING_ENABLED=true`) — billing is built & dormant.
- **Email provider** key (`RESEND_API_KEY` or SMTP_*) — password-reset + welcome + trial-reminder
  emails are wired and no-op safely until this is set.
- **Sentry DSN** — error tracking wired (web+worker), off until DSN set.
- **Facebook**: residential proxy (`FB_PROXY_*`) + a SECONDARY account BEFORE any FB login from the
  Hetzner IP (permanent-ban risk — see DO_NOT.md). One-time login via cabinet → Каналы → Facebook.
- **Telegram**: phone + SMS code for the userbot session (owner does once).
- Social links for the footer (msg: "ссылки у MacBook") — if you have them, drop into the landing footer.

## Done today (continued — all on main + prod, verified)
- RAM: 4G host swapfile live (swappiness=10) + per-service `mem_limit` ceilings in
  docker-compose.prod.yml (apply on deploy; verified 384m on scheduler / 768m on web).
- Trial-ending reminder emails (3d/1d/expired) via scheduler — new `trial_reminders`
  table, deduped per (user,stage); smoke-verified end-to-end (no-op until email key).
- Auto-organize TG/FB groups by theme: `common/group_theme.py` (HE/RU/EN classifier,
  15/15 cases) tags each channel with a vertical + sorts; theme chip renders in the
  cabinet (verified by screenshot, 0 JS errors). Display-only — sync/routing untouched.

## Deferred (do NOT rush — post-launch, focused)
- **Alembic migrations baseline.** Needs init + env.py + autogenerate + STAMP prod as
  baseline, else first `upgrade head` recreates existing tables. Current `schema.py`
  (create_all + idempotent ALTER) works and is how new tables/columns shipped this
  session. Set up Alembic deliberately after launch, then retire the ALTER shim.

## Owner-gated reminders (unchanged) — see section above.

## SSH
`ssh -i ~/.ssh/posting_autopilot_hetzner root@167.233.98.210` → `/opt/posting-autopilot`.
