# Ночной отчёт — Posting Autopilot, 2026-06-01

Шлифовка по обратной связи от 5 симуляций новых юзеров. 13 коммитов, 63 файла,
+2380/−300. **Ничего не опубликовано и не задеплоено** — только код + локальные коммиты.
Push держу до тебя (по твоей просьбе «ничего не публикуй до утра»).

Всё проверено реально (Flask test client / live curl), не на словах. Реальные посты в
FB/TG и отправка сообщений сегодня НЕ запускались.

---

## Что сделано (по приоритету из roadmap)

| # | Что | Коммит | Проверено |
|---|-----|--------|-----------|
| WIP | Спасён незакоммиченный WIP прошлой сессии (bcrypt-пароли, харднинг secret-key, rate-limit, i18n). Заодно починил **сломанный HEAD** — он ссылался на 4 незакоммиченных модуля, чистый checkout падал | `50acc0a` | app грузится, 7 страниц 200 |
| **#1** | **Telegram по телефону** — убрал стену «API ID / API Hash» (убийца №1, ~65% отвала). Юзер вводит только телефон, BYO уходит в «Дополнительно» | `d59bfef` | shared→телефон-first; BYO→как было |
| #5 | Авто-создание кампании (убрал концепт «кампания») + постинг без воркера (`ALLOW_INLINE_POSTING`, по умолч. OFF) + дружелюбные сообщения вместо «Redis offline» | `9d37aa9` | идемпотентность + inline-фоллбэк |
| #10 | Кнопка «Отправить тестовый лид мне в Telegram» + **починил баг**: живой listener игнорировал `owner_telegram_id` из профиля | `c2aba95` | отправка (замокана), резолвер |
| #14 | ROI-квитанция на дашборде: постов / откликов / горячих / «~N часов сэкономлено» | `0296dbc` | 5 постов→0.9ч, карточка рендерится |
| #9 | Честная панель бан-безопасности (реальные тайминги) + убрал claim «без банов» с лендинга (противоречил terms §6) | `408f57e` | панель на 2 стр., лендинг чист |
| #7 | Детект языка по Accept-Language (RU/EN трафик больше не падает на иврит) + перевод register.html и хардкодов дашборда | `f83283b` | ru/en/he детектятся |
| #6/#7 | Нейтральные ниш-термины: FAQ pricing переписан (объявления/отклики, не вакансии/кандидаты) + 3 заголовка | `21cfa5b` | FAQ переведён в 3 языках |
| **#2** | **Кнопка «Подключить мой Facebook»** для браузер-входа (раньше только из терминала). Gated `FB_ALLOW_LOCAL_CAPTURE` (OFF → инструкция, ничего не запускает) | `813a1ad` | gate-off безопасен, браузер не спавнится |
| #15 | Убрал из git 4 закоммиченных `cookies.txt` (демо-токен) + `docs/SECURITY_NOTES.md` с follow-up'ами | `1c135f2` | 0 cookies в трекинге |
| #11 | WhatsApp click-to-chat — он **уже был** в реальном hot-lead (0…→972…); добавил его и в тестовую квитанцию для консистентности | `e402b1c` | wa.me в 3 языках |
| landing | Ниш-полоса «Не только найм»: 👷 работа 🚗 авто 🏠 квартиры 🔧 услуги 🛒 marketplace | `5822e73` | рендер в 3 языках |
| test | Сделал phase2-тест i18n-корректным (грепал английский литерал) | `572d686` | phase2: 41/0 |

**Проверка:** live-смоук 11 роутов × 3 языка = 33 запроса, все 200, ноль 500-х.
Тесты: phase2 41/0. Остались 3 преждевременных падения НЕ по моей вине (см. ниже).

---

## ⚠️ Что нужно от тебя (я физически не могу — нужны твои аккаунты/решения)

1. **Push в GitHub** — 13 коммитов готовы локально, ждут твоего ОК (держу по твоей просьбе).
2. **Telegram-по-телефону включится**, когда поставишь 2 переменные (регистрация ОДНОГО
   приложения на my.telegram.org, один раз): `RECRUITBOT_TG_API_ID`, `RECRUITBOT_TG_API_HASH`.
   Без них — fallback на старый BYO-режим, ничего не ломается.
3. **Деплой + Stripe** (чтобы «попробовать → заплатить» закрылось): нужен VPS + домен
   `posting-autopilot.com` (ты купил) + Stripe-аккаунт. Runbook'и готовы.
4. **FB-вход кнопкой** заработает на твоей машине, если поставишь `FB_ALLOW_LOCAL_CAPTURE=1`
   (на сервере НЕ ставить — нужен видимый браузер).
5. **Постинг без воркера**: `ALLOW_INLINE_POSTING=1` — «Постить» сработает и без отдельного
   воркер-процесса (по умолчанию выключено, чтобы не было сюрприз-публикаций).

## Преждевременные падения тестов (НЕ мои регрессии — проверил против прошлого коммита)
- phase1: тест ждёт `POST_DELAY_MIN>=60`, в коде намеренно `30` (прошлая сессия, UX пилота).
- phase1: проверка схемы через Postgres `information_schema` на SQLite.
- phase3: дальше по чеклисту ждёт «sticky nav» — это лендинг-спека, гнаться вслепую ночью
  за полным редизайном не стал (verticals добавил как on-strategy).

## Не трогал намеренно (нужно твоё решение / риск)
- Реальный постинг в FB/TG, деплой, Stripe live, отправка сообщений — по твоей директиве.
- Шифрование сессий/PII at-rest, переписывание git-истории, data/refund-заявления —
  описал в `docs/SECURITY_NOTES.md` (трогает живой путь загрузки сессий / деструктивно).
- FB Marketplace реальный постинг (#12), захват ответов со стороны FB (#13) — крупные
  gray-zone фичи, отдельный заход.
